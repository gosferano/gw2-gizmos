<Page x:Class="Gw2Gizmos.Desktop.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:c="clr-namespace:Gw2Gizmos.Desktop.Controls"
      TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      ScrollViewer.CanContentScroll="False"
      Title="Logs">
    <c:PageShell Title="Logs" Scrollable="False">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <ComboBox ItemsSource="{Binding Levels}" SelectedItem="{Binding LevelFilter}" Width="90"
                          Height="{StaticResource FormControlHeight}" />
                <CheckBox Content="Desktop" IsChecked="{Binding ShowDesktop}" Margin="12,0,0,0" VerticalAlignment="Center" />
                <CheckBox Content="Worker" IsChecked="{Binding ShowWorker}" Margin="8,0,0,0" VerticalAlignment="Center" />
                <ui:TextBox PlaceholderText="filter text" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                            Width="200" Height="{StaticResource FormControlHeight}" Margin="12,0,0,0" />
            </StackPanel>

            <DataGrid Grid.Row="1" x:Name="LogList" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserResizeRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent"
                      FontFamily="Consolas" FontSize="12"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Width="92"
                                        Binding="{Binding Timestamp, StringFormat='HH:mm:ss.fff'}" />
                    <DataGridTextColumn Header="Lvl" Width="46" Binding="{Binding Level}" />
                    <DataGridTextColumn Header="Source" Width="70" Binding="{Binding Source}" />
                    <DataGridTextColumn Header="Message" Width="*" Binding="{Binding Message}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </c:PageShell>
</Page>
