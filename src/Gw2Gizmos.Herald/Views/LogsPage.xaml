<Page x:Class="Gw2Gizmos.Herald.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="Logs">
    <Grid Margin="16"
          MaxHeight="{Binding (ScrollViewer.ViewportHeight), RelativeSource={RelativeSource AncestorType=ScrollViewer}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Logs" FontSize="22" FontWeight="Medium" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{Binding Levels}" SelectedItem="{Binding LevelFilter}" Width="90" Margin="16,0,0,0" />
            <CheckBox Content="Herald" IsChecked="{Binding ShowHerald}" Margin="12,0,0,0" VerticalAlignment="Center" />
            <CheckBox Content="Worker" IsChecked="{Binding ShowWorker}" Margin="8,0,0,0" VerticalAlignment="Center" />
            <ui:TextBox PlaceholderText="filter text" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                        Width="180" Margin="12,0,0,0" />
        </StackPanel>

        <ListView Grid.Row="1" x:Name="LogList" ItemsSource="{Binding Entries}" FontFamily="Consolas" FontSize="12"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" Width="86"
                                    DisplayMemberBinding="{Binding Timestamp, StringFormat='HH:mm:ss.fff'}" />
                    <GridViewColumn Header="Lvl" Width="44" DisplayMemberBinding="{Binding Level}" />
                    <GridViewColumn Header="Source" Width="66" DisplayMemberBinding="{Binding Source}" />
                    <GridViewColumn Header="Message" Width="560" DisplayMemberBinding="{Binding Message}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
